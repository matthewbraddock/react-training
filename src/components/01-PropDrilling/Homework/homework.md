# Homework

- Look up if and how to use a callback with context to avoid prop drilling.
  - Context make testing a lot harder than prop drilling.

- Learn about the virtual DOM and how React uses it to optimize rendering.
  - Maybe can learn something Peter doesn't know :P
- React fiber
  - Maybe can learn something Peter doesn't know :P
